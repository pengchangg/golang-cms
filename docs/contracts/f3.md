# F3 冻结契约

- 冻结版本：F3
- 状态：已冻结并集成
- 适用范围：阶段三 S3 兼容对象存储素材、媒体字段、通用后台任务、CSV 导入导出和素材客户端路由
- 依据：`docs/cms-design.md`、`docs/development-plan.md`、`docs/contracts/f0.md`、`docs/contracts/f1.md`、`docs/contracts/f2.md` 和当前阶段二实现
- OpenAPI 分片：`api/openapi/fragments/admin/assets/**`、`admin/transfers/**`、`admin/jobs/**`、`content/assets/**`
- 集成状态：本文新增分片均标记 `x-integration-status: integrated` 并聚合进根 OpenAPI

F3 只做 F0 至 F2 的兼容扩展，不改变现有管理内容 DTO、客户端内容查询、分页、展开或 ETag 语义。本文冻结契约；实现可按本文聚合根 OpenAPI、注册运行时路由并修改 Go 代码、Web 代码或迁移。

## 1. S3 兼容对象存储边界和生产配置

### 1.1 私有 Bucket

- 素材、CSV 导入源文件、错误报告和导出结果全部保存在同一个私有 S3 兼容对象存储 Bucket；Bucket 禁止公共读和公共写。
- 浏览器只使用服务端签发的短时 URL 直传或下载。签名 URL、Access Key、Session Token 和签名请求头不得持久化到业务表、日志、审计或前端存储。
- CMS 数据库保存稳定对象 Key，不保存对象存储 URL。所有对外下载请求每次重新鉴权并生成新签名。
- 生产启动必须验证 Endpoint 和 Bucket 可访问；提供方支持 Location/ACL API 时同时验证 Region 和公共授权；始终以实际匿名读写请求确认 Bucket 拒绝公共访问，并验证凭证可执行 `PutObject`、`HeadObject`、`GetObject` 和受限删除操作。检查失败时启动失败。

生产配置固定为：

| 环境变量 | 必填 | 规则 |
| --- | --- | --- |
| `S3_ENDPOINT` | 是 | S3 兼容 HTTPS Endpoint，不接受用户请求覆盖 |
| `S3_REGION` | 是 | 与 Bucket 所在 Region 一致 |
| `S3_BUCKET` | 是 | 私有 Bucket 名称 |
| `S3_ACCESS_KEY_ID` | 是 | 仅应用入口读取 |
| `S3_ACCESS_KEY_SECRET` | 是 | 仅应用入口读取，不得输出 |
| `S3_SESSION_TOKEN` | 否 | 使用临时凭证时填写，并与 Access Key 同生命周期 |
| `S3_USE_PATH_STYLE` | 否 | 默认 `false`；仅兼容实现明确要求时设为 `true` |
| `S3_BUCKET_ENDPOINT` | 否 | 默认 `false`；Endpoint 已绑定单个 Bucket 的 CNAME 场景设为 `true`，不得与 path style 同时启用 |
| `S3_UPLOAD_URL_TTL` | 否 | 默认 15 分钟，允许 1 至 30 分钟 |
| `S3_DOWNLOAD_URL_TTL` | 否 | 默认 5 分钟，允许 1 至 15 分钟 |

生产环境不得使用仓库默认凭证、公共 Bucket、HTTP Endpoint 或无限期签名。应用不负责创建 Bucket、修改 Bucket ACL 或签发跨 Bucket 权限。

阿里云部署使用官方 S3 兼容 Endpoint和 virtual-host style，即 `S3_USE_PATH_STYLE=false`；Bucket 绑定 CNAME 时改用 CNAME Endpoint，并设置 `S3_BUCKET_ENDPOINT=true`。浏览器直传的 Bucket CORS 必须允许 CMS Origin、`PUT` 及 `Content-Type`、`x-amz-meta-sha256`、`If-None-Match` Header。

### 1.2 S3 Adapter

素材和导入导出模块只依赖提供方无关的 Adapter。Adapter 使用 AWS SDK for Go v2 的 S3 客户端和 PresignClient，不得手写请求签名；SDK 只存在于 Adapter 实现。公开语义冻结如下，具体 Go 包名可按领域所有权落位：

```go
type ObjectStore interface {
	SignPut(ctx context.Context, req SignPutRequest) (SignedRequest, error)
	Head(ctx context.Context, objectKey string) (ObjectMetadata, error)
	SignGet(ctx context.Context, req SignGetRequest) (SignedRequest, error)
	Put(ctx context.Context, req PutObjectRequest, body io.Reader) (ObjectMetadata, error)
	Get(ctx context.Context, objectKey string) (io.ReadCloser, ObjectMetadata, error)
	Delete(ctx context.Context, objectKey string) error
}

type SignPutRequest struct {
	ObjectKey   string
	ContentType string
	Size        int64
	SHA256      string
	ExpiresAt   time.Time
}

type SignGetRequest struct {
	ObjectKey         string
	DownloadFilename string
	ExpiresAt         time.Time
}

type PutObjectRequest struct {
	ObjectKey   string
	ContentType string
	Size        int64
	SHA256      string
}

type SignedRequest struct {
	Method    string
	URL       string
	Headers   map[string]string
	ExpiresAt time.Time
}

type ObjectMetadata struct {
	ObjectKey    string
	Size         int64
	ContentType  string
	SHA256       string
	ETag         string
	LastModified time.Time
}
```

- `SignPut` 只签发 `PUT`，并把 `Content-Type`、`If-None-Match: *` 和标准 S3 用户元数据 `Metadata["sha256"]` 纳入签名；该元数据在 HTTP 中表示为 `x-amz-meta-sha256`。签名 URL 不得被重放覆盖同一对象。浏览器不可控的 `Content-Length` 不得签名或作为前端必设 Header，大小由确认阶段的 `Head` 与受限流式 `Get` 校验；`SignGet` 只签发 `GET`。
- `ExpiresAt` 必须由调用方按类型化 TTL 计算，Adapter 拒绝过去时间或超过配置上限的时间；`DownloadFilename` 只用于安全生成 `Content-Disposition`，不得改变对象 Key。
- `Put` 必须按 `Size` 限制读取并提交规范 `Content-Type` 和 S3 用户元数据 `Metadata["sha256"]`，实际读取字节数不等于 `Size` 时失败并清理未完成对象。
- `Head` 的对象不存在错误必须可与临时对象存储故障区分。网络、限流和 S3 兼容服务 5xx 映射为可重试 `object_store_unavailable`；签名或权限配置错误映射为不可重试内部配置错误，不向调用方暴露凭证细节。
- `Put` 和 `Get` 用于 Worker 流式读写 CSV 文件，不得把完整对象载入内存。`Delete` 只清理尚未确认的上传对象和已过期任务文件，不用于物理删除素材对象。
- `ObjectMetadata.SHA256` 来自已签名的 S3 用户元数据 `Metadata["sha256"]`，只用于确认上传声明没有被替换。确认素材和导入源文件时，服务端还必须通过 `Get` 流式计算对象实际 SHA-256 并与申请值比较，不能仅信任客户端可写的对象元数据。V1 不把 ETag 当作 MD5 或 SHA-256；ETag 仅作为独立的不透明元数据保存和比较。
- Adapter 的 Fake 必须覆盖对象不存在、元数据不匹配、签名过期、临时故障和流式读写，不依赖真实企业对象存储。

## 2. 素材对象、Key 和状态

### 2.1 对象 Key

- 对象 Key 只能由服务端生成，固定格式为 `assets/{asset_id}/{32位小写十六进制随机值}`。`asset_id` 和随机值均在创建上传申请时确定。
- 用户文件名不得出现在对象 Key 中，不得接受调用方传入完整或部分 Key，也不得从文件扩展名推断 MIME。
- CSV 文件使用独立命名空间：任务创建前的导入源文件为 `transfers/uploads/{upload_id}/{32位随机值}.csv`；导入错误报告和导出结果位于 `transfers/{job_id}/attempt-{attempt}-{lease摘要}/{errors|result}.csv`。
- Key 是内部存储定位符，不通过素材或任务 DTO 返回；对外只返回 CMS 资源 ID 和短时签名结果。

### 2.2 素材状态和元数据

素材状态固定为：

```text
quarantined
available
archived
```

- 创建上传申请时即创建 `quarantined` 素材。该状态不可用于内容值、不可发布、不可下载。
- 上传确认成功后原子写入确认元数据并转换为 `available`。确认失败保持 `quarantined`，允许在上传申请过期前重新直传和确认。
- `available -> archived` 是唯一归档转换，不可恢复。归档不物理删除对象，也不改写历史 Revision 或当前已发布 Revision。
- 新 Revision 只能引用 `available` 素材。发布前必须在发布事务中重新验证候选 Revision 的全部素材仍为 `available`；任一素材隔离、归档或不存在时返回 `409 asset_not_publishable`，发布指针不切换。
- 已经在线的 Revision 所引用素材后来归档时继续可下载，避免静默破坏线上内容；归档素材不能进入任何新 Revision，也不能随新 Revision 再次发布。
- 病毒扫描不属于 V1。`quarantined` 仅表示上传尚未确认，为后续扫描扩展预留状态。

素材固定保存并返回以下非敏感元数据：`id`、`filename`、`mime_type`、`size`、`sha256`、`etag`、`status`、`created_by`、`created_at`、`confirmed_at`、`archived_at`。其中：

- `filename` 是去除路径后的原始显示文件名，长度 1 至 255 个 Unicode 码点，禁止 NUL、`/` 和 `\`。
- `mime_type` 是小写规范 MIME，最长 255 字节；允许列表由类型化配置注入，V1 不接受 `application/octet-stream` 作为绕过值。
- `size` 是字节数，必须为 1 至配置上限；配置上限不能超过 5 GiB。
- `sha256` 是 64 位小写十六进制。客户端在申请上传前计算，上传时作为签名元数据提交；确认时必须与 `Head` 返回值一致。
- `etag` 是去除对象存储响应外围引号后的非空不透明字符串。不得用它替代 SHA-256 完整性字段。

### 2.3 上传申请和确认

上传流程固定为：

1. `POST /api/admin/v1/assets/uploads` 提交 `filename`、`mime_type`、`size`、`sha256`。
2. 服务端校验 `assets.upload`、元数据和配额，创建 `quarantined` 素材并返回 `AssetUpload`。
3. 浏览器按响应中的 `method`、`url` 和全部 `headers` 直接 `PUT`；不得增删已签名头。
4. `POST /api/admin/v1/assets/{asset_id}/confirm` 携带空 JSON object。
5. 服务端 `Head` 固定 Key，逐项比较对象大小、规范 MIME、SHA-256 元数据和非空 ETag，再通过 `Get` 流式计算实际 SHA-256；完全一致后转为 `available`。

- 上传 URL 在 `expires_at` 失效。过期后返回 `409 asset_upload_expired`，调用方必须创建新素材上传申请，不能为同一素材续签。
- 确认是幂等的：已经成功确认且对象元数据仍匹配时返回同一个 `Asset`；归档素材不能确认。
- 对象不存在返回 `409 asset_object_missing`；大小、MIME、声明 SHA-256、实际 SHA-256 或 ETag 不一致返回 `409 asset_metadata_mismatch`；临时对象存储故障返回 `503 object_store_unavailable` 并允许重试。
- 元数据不一致不得自动采用对象存储实际值，不得把素材设为可用。过期或长期未确认对象由清理任务处理。

### 2.4 下载

- 管理端 `GET /api/admin/v1/assets/{asset_id}/download` 要求 `assets.view`，`available` 和 `archived` 可下载，`quarantined` 返回 `409 asset_not_available`。
- 客户端 `GET /api/content/v1/assets/{asset_id}` 要求有效 API Key，并且该素材被至少一个“API Key 授权模型的当前发布 Revision”引用。其他情况统一返回 `404 published_asset_not_found`，不得泄露素材或模型存在性。
- 下载成功返回 `302`，`Location` 是短时对象存储签名 URL，同时返回 `Cache-Control: private, no-store`。响应和审计不得记录 `Location` 完整值。
- 每次下载都重新检查当前权限、API Key 状态、发布指针和素材状态。管理权限或模型范围撤销后，旧 CMS 路由请求立即失效；已经签发的对象存储 URL 只存活到其短 TTL 到期。

## 3. 媒体 JSON 和 Revision 引用

媒体字段内容值固定为：

| 字段类型 | JSON 表示 |
| --- | --- |
| `single_media` | 素材 ID string；缺省或 `null` 表示无素材 |
| `multi_media` | 素材 ID array；顺序有意义，不允许重复，最多 50 项 |

- 媒体字段可位于模型允许的任意深度，包括对象和重复组。数组、对象位置通过 RFC 6901 JSON Pointer 定位，例如 `/gallery/0/image`。
- `asset_references` 是 Revision 级不可变引用。每行至少保存 `revision_id`、源 `entry_id/model_id`、`field_id`、`asset_id`、字段值的 `json_pointer` 和从 0 开始的 `position`。
- 单媒体固定 `position=0`；多媒体按数组顺序保存。`(revision_id,json_pointer,position)` 唯一，同一多媒体值中的素材 ID 不得重复。
- 创建 Revision 时，内容模块先收集全部媒体值，通过素材服务批量验证 `available`，再在同一 MySQL 事务插入 Revision JSON、投影、关联、素材引用、指针和成功审计。任一步失败整体回滚。
- 历史 Revision 的引用不可更新或删除。草稿指针和发布指针变化不复制引用；可见性通过指针连接目标 Revision 判断。
- 素材模块拥有 `asset_references` 表。内容模块只使用冻结的 `ValidateAvailable`、`InsertRevisionReferences` 和 `ValidatePublishableRevision` 语义接口，不直接访问素材表；接口必须接受调用方显式事务执行器。
- 发布锁顺序延续 F2 的 `model -> entry -> revision/pointer`，之后按 `asset_id ASC` 锁定候选素材并校验状态，避免并发归档造成检查后发布竞态。
- 客户端内容 JSON 保留素材 ID，不返回对象存储 URL。调用方通过独立客户端素材路由换取短时下载地址；素材下载不参与内容响应 ETag 计算。

## 4. 通用后台任务

### 4.1 状态和字段

通用任务状态固定为：

```text
queued
running
succeeded
failed
canceled
```

任务类型固定为 `csv_import`、`csv_export`。任务公共 DTO 至少包含 `id`、`type`、`status`、`model_id`、`progress`、`attempt`、`max_attempts`、`cancel_requested_at`、`error_code`、`error_message`、`created_by`、`created_at`、`started_at`、`finished_at` 和 `expires_at`。

- `progress` 是 0 至 100 的整数，只用于展示，不作为提交或完成依据；同一次领取内不得倒退。
- `attempt` 从 0 开始；每次成功领取递增。`max_attempts` 固定为 3，包括第一次执行。
- `error_code` 是稳定机器码，`error_message` 是已脱敏摘要。仅 `failed` 可返回非空错误；不得包含 CSV 原始行、对象存储 URL、SQL 或凭证。
- `succeeded`、`failed`、`canceled` 是终态。仅显式重试可把符合条件的 `failed` 任务重新排队；终态结果不能被过期 Worker 覆盖。

合法转换固定为：

| 当前状态 | 事件 | 目标状态 |
| --- | --- | --- |
| `queued` | 有效领取 | `running` |
| `queued` | 用户取消 | `canceled` |
| `running` | 执行成功 | `succeeded` |
| `running` | 不可重试失败或耗尽次数 | `failed` |
| `running` | 可重试失败且尚有次数 | `queued` |
| `running` | 已请求取消并到达安全检查点 | `canceled` |
| `running` | 租约过期且尚有次数 | `queued` |
| `running` | 租约过期且耗尽次数 | `failed` |
| `failed` | 用户显式重试且尚有次数 | `queued` |

### 4.2 领取、租约和续租

- Worker 使用 MySQL 事务和 `SELECT ... FOR UPDATE SKIP LOCKED` 按 `available_at ASC,id ASC` 领取一个 `queued` 任务，条件更新为 `running`，递增 `attempt`，写入随机高熵 `lease_token`、`lease_owner` 和 `lease_expires_at` 后提交。
- 租约固定 60 秒，Worker 每 20 秒续租。续租、进度、完成、失败和取消确认必须同时匹配 `id`、`status=running`、`lease_token` 且租约尚未过期。
- `lease_token` 只返回给 Worker 内部调用，不进入管理 DTO、日志或审计。任务框架不提供公网领取、续租或完成 HTTP 路由。
- Worker 失去租约后必须停止产生副作用，不得更新任务。回收器将过期 `running` 任务按转换表重新排队或标记失败，并清除租约字段。
- 自动重试延迟固定为第一次失败后 30 秒、第二次失败后 2 分钟。取消、校验失败、权限快照非法和数据冲突不可自动重试；网络超时、S3 兼容服务 429/5xx 和可判定的临时数据库故障可重试。
- Worker 优雅停止时停止领取新任务，并在进程退出前尽力续租正在处理的任务；不得把进程内 goroutine 状态当作任务真相。

### 4.3 取消、重试和幂等

- `POST /api/admin/v1/jobs/{job_id}/cancel` 对 `queued` 任务立即设为 `canceled`；对 `running` 任务只设置 `cancel_requested_at`，Worker 在事务批次之间检查并安全终止。已设置取消请求时重复调用返回当前 Job。
- 已终态任务取消返回 `409 job_not_cancelable`。CSV 正式写入事务开始后不得响应取消，事务完成后任务成功；避免出现任务显示取消但正式内容已经写入。
- `POST /api/admin/v1/jobs/{job_id}/retry` 只接受 `failed` 且 `attempt < max_attempts` 的任务；清空终态错误、设置 `queued`，但保留同一任务 ID、输入快照和已使用次数。否则返回 `409 job_not_retryable`。
- 创建导入和导出任务必须携带 `Idempotency-Key`，长度 16 至 128 个可见 ASCII 字符。幂等范围是 `actor_id + HTTP method + path + key`，保存规范请求 SHA-256 和首次响应 24 小时。
- 同一范围、同一请求重放返回原任务和 `200`；同一 Key 请求体不同返回 `409 idempotency_key_reused`。幂等记录和任务创建处于同一事务，不依赖进程内缓存。

## 5. CSV 编码

### 5.1 文件和表头

- CSV 输入为 UTF-8，可带或不带 UTF-8 BOM；输出固定带 UTF-8 BOM。输入包含非法 UTF-8、NUL 或 BOM 出现在文件开头以外时失败。
- 分隔符固定为逗号 `,`，记录分隔接受 `LF` 和 `CRLF`，输出固定 `CRLF`。字段引用和双引号转义遵循 RFC 4180。
- 第一行是表头，必须与任务创建时模型快照中的全部 active 根字段 key 完全相同，顺序固定为模型字段定义顺序。不得使用显示名称，不得缺列、增列、重复列或重排。
- 模板只包含 BOM 和表头，不包含示例数据。单文件最多 100,000 个数据行，表头不计入；超过后以 `csv_row_limit_exceeded` 失败。
- 每个物理字段最大 10 MiB，每个逻辑记录最大 20 MiB。解析器必须流式读取，不能将整个 CSV 或全部规范化行加载到内存。

### 5.2 各字段编码

空 CSV 单元格表示字段缺失；非空单元格按下表解析。JSON 字面量 `null` 只对 `json`、`object` 和 `repeatable_group` 表示实际 JSON null。必填字段缺失仍按普通内容校验失败。

| 字段类型 | CSV 单元格编码 |
| --- | --- |
| `single_line_text`、`multi_line_text` | JSON string；空字符串写为 `""`，从而与缺失单元格区分 |
| `rich_text` | 符合 F1 `RichTextDocument` 的紧凑 JSON object string |
| `integer` | 无前导加号的规范十进制整数 |
| `decimal` | F1 规范十进制 string，不接受指数 |
| `boolean` | 小写 `true` 或 `false` |
| `date` | `YYYY-MM-DD` |
| `datetime` | RFC 3339，导入后归一化 UTC |
| `single_select` | 稳定枚举 `value` |
| `multi_select` | 枚举 `value` 的紧凑 JSON string array |
| `json` | 任意合法紧凑 JSON value string |
| `single_media` | 素材 ID string |
| `multi_media` | 素材 ID 的紧凑 JSON string array |
| `single_relation` | 目标内容条目 ID string |
| `multi_relation` | 目标内容条目 ID 的紧凑 JSON string array |
| `object` | 以 child key 为属性名、值继续遵循字段 JSON 编码的紧凑 JSON object string |
| `repeatable_group` | 以 child key 为属性名的紧凑 JSON object array string |

- “紧凑 JSON”指无语义外空白的 UTF-8 JSON；对象属性按模型 child 定义顺序输出，导入接受任意属性顺序但拒绝重复键、未知键和非规范字段值。
- 多值数组顺序有意义并禁止重复；关联和媒体 ID 都是不透明字符串，不接受 URL、文件名、模型 key 或显示名称替代。
- 导出缺失值输出空单元格；JSON 实际 `null` 输出 `null`。所有其他值按上表规范编码，再按 RFC 4180 执行 CSV 引号转义，保证导出文件可作为同一未破坏性变更模型的导入输入。

## 6. CSV 导入

### 6.1 上传和创建

- `POST /api/admin/v1/models/{model_id}/imports/uploads` 要求 `transfers.execute` 和目标模型 `content.create`，申请 CSV 源文件直传，返回短时签名和 `upload_id`。
- `POST /api/admin/v1/models/{model_id}/imports` 要求同样权限、`Idempotency-Key` 和已上传 `upload_id`。服务端 `Head` 校验对象、大小和 SHA-256 元数据，并通过 `Get` 流式计算实际 SHA-256 后创建 `csv_import` 任务。
- 创建时固定模型版本、字段树、调用者 ID 和权限需求。导入执行前再次检查调用者仍启用且仍有 `transfers.execute` 与目标模型 `content.create`；权限撤销则任务失败为 `permission_denied`。
- 一个 `upload_id` 只能绑定一个逻辑任务；幂等重放返回原任务。源文件默认在任务终态后保留 7 天，然后允许清理。

### 6.2 暂存、全量校验和正式写入

导入固定执行以下阶段：

1. 从对象存储流式读取并解析 CSV，将每行规范化内容及行号分批写入任务专属暂存表。
2. 对全部暂存行执行模型、类型、必填、唯一性、行间唯一性、关联、媒体状态和对象结构校验。
3. 任一行失败时不写正式内容，生成完整错误报告后将任务标记 `failed`。
4. 全部行通过后，在一个 MySQL 事务中分批调用普通内容写入语义，创建草稿条目、首个不可变 Revision、投影、关联、素材引用、唯一占用和成功审计。
5. 正式事务提交后任务才可标记 `succeeded`；随后清理暂存行。提交失败整体回滚，按错误可重试性处理。

- “分批”只限制 SQL 参数和内存，不改变正式写入的单事务原子性。100,000 行必须通过容量测试确认事务和锁资源可接受。
- 导入不能调用提交、审核或发布动作，所有新 Revision 状态为 `draft`。
- 正式写入前必须重新校验与现有正式数据相关的唯一值、关联和素材状态，防止暂存校验后的并发变化。
- 失败重试必须清理或覆盖同一任务上次 attempt 的暂存数据；正式事务成功后即使任务终态更新暂时失败，也必须通过持久完成标记恢复为成功，禁止再次写入内容。

### 6.3 错误报告

- 错误详情固定为 `row`、`field`、`code`、`message`。`row` 是 CSV 中从 2 开始的物理记录号；文件级错误使用 `row=1`，`field` 为空字符串。
- 同一行可返回多个独立错误，按 `row ASC,field ASC,code ASC` 排序。单任务最多记录 100,000 条详情；超出时任务仍失败，并设置 `errors_truncated=true`。
- 稳定导入错误代码至少包括 `invalid_csv`、`invalid_header`、`csv_row_limit_exceeded`、`invalid_encoding`、`validation_failed`、`not_unique`、`relation_not_found`、`asset_not_available` 和 `model_changed`。
- 错误报告是带 UTF-8 BOM 的 CSV，表头固定为 `row,field,code,message`，保存到对象存储，有效期为任务终态后 7 天。

## 7. CSV 导出

- `POST /api/admin/v1/models/{model_id}/exports` 要求 `transfers.execute`、目标模型 `content.view` 和 `Idempotency-Key`。请求可携带 F2 管理列表同语义的 `workflow_status`、`filter`、`relation_filter` 和 `sort`，不接受 `cursor`、`limit`、`expand` 或 `include_total`。
- 创建任务时保存规范化筛选和排序的不可变快照、模型字段快照、调用者 ID 和权限需求。后续模型显示名变化不影响任务；字段结构发生不兼容变化时任务失败为 `model_changed`，不得静默改变列。
- Worker 执行前重新检查调用者仍启用且仍有 `transfers.execute` 和目标模型 `content.view`。查询只包含该权限下管理 API 可见的条目和快照条件，不提供客户端发布视图替代管理导出。
- 导出按稳定排序流式读取并流式写入对象存储，首字节固定 UTF-8 BOM，表头和字段编码遵循第 5 节。不得先在内存构造完整文件。
- 成功结果默认在任务完成后 7 天过期。`GET /api/admin/v1/jobs/{job_id}/files/result` 下载前重新检查当前用户仍有 `transfers.download`、任务所属模型 `content.view`、文件未过期且任务归属可见。
- 错误报告下载要求 `transfers.download` 和任务原执行权限；过期返回 `410 job_file_expired`，权限撤销返回 `403 permission_denied`，不存在或不属于可见任务返回 `404 resource_not_found`。
- 下载成功返回 `302` 和短时签名 `Location`，使用 `Cache-Control: private, no-store`。任务文件的签名 URL 不进入任务 DTO。

## 8. 管理和客户端路由

### 8.1 管理素材

```text
GET  /api/admin/v1/assets
POST /api/admin/v1/assets/uploads
GET  /api/admin/v1/assets/{asset_id}
PATCH /api/admin/v1/assets/{asset_id}
DELETE /api/admin/v1/assets/{asset_id}
POST /api/admin/v1/assets/{asset_id}/confirm
GET  /api/admin/v1/assets/{asset_id}/download
```

- 列表、详情和下载要求 `assets.view`；上传申请和确认要求 `assets.upload`；`PATCH` 只允许修改 `filename` 并要求 `assets.update`；归档要求 `assets.archive`。
- 列表可按状态和规范 MIME 精确筛选，按 `created_at DESC,id DESC` 使用 F1 游标外壳。
- 归档成功返回 `204`；重复归档返回 `409 resource_archived`。归档动作、上传确认和下载按设计写入审计，审计不含对象 Key或签名 URL。

### 8.2 管理任务和传输

```text
GET  /api/admin/v1/models/{model_id}/transfers/template
POST /api/admin/v1/models/{model_id}/imports/uploads
POST /api/admin/v1/models/{model_id}/imports
POST /api/admin/v1/models/{model_id}/exports
GET  /api/admin/v1/jobs
GET  /api/admin/v1/jobs/{job_id}
POST /api/admin/v1/jobs/{job_id}/cancel
POST /api/admin/v1/jobs/{job_id}/retry
GET  /api/admin/v1/jobs/{job_id}/errors
GET  /api/admin/v1/jobs/{job_id}/files/{file_kind}
```

- 模板要求 `transfers.download`、目标模型 `content.view`，同步返回 `text/csv; charset=utf-8` 文件。
- 任务列表和详情要求 `transfers.execute`，只返回当前用户创建的任务；应急管理员可查看全部任务。列表按 `created_at DESC,id DESC`，可按 `type`、`status`、`model_id` 筛选。
- 取消和重试只允许任务创建者或应急管理员，并重新检查对应执行权限。错误分页接口返回结构化 `TransferError`；文件下载额外要求 `transfers.download`。
- `file_kind` 只允许 `errors` 或 `result`，并分别要求任务存在对应文件。导入源文件不提供管理下载路由。

### 8.3 客户端素材

```text
GET /api/content/v1/assets/{asset_id}
```

客户端 API 不提供素材列表、元数据详情、上传、归档或任意写路由。成功仅返回 `302`，不返回 `Asset` DTO。

客户端素材路由只由生产集成层 `ClientAssetHandler` 注册；素材模块 Handler 只注册 8.1 的管理路由，不持有客户端 API Key scope provider。

## 9. 路由 DTO 和稳定错误码

统一错误外壳沿用 F0，校验详情沿用 F1 `{path,code,message}`。F3 新增稳定顶层错误码：

| HTTP | `error.code` | 适用情况 |
| --- | --- | --- |
| 400 | `validation_failed` | 素材元数据、导入导出请求或 CSV 字段校验失败 |
| 400 | `invalid_cursor` | 素材、任务或错误详情游标无效 |
| 400 | `invalid_query` | 导出筛选或任务列表参数无效 |
| 401 | F1/F2 既有认证码 | 管理会话或 API Key 无效 |
| 403 | `permission_denied` | 管理用户缺少素材、传输或模型权限 |
| 404 | `resource_not_found` | 管理端素材、上传、任务或模型不存在 |
| 404 | `published_asset_not_found` | 客户端素材不存在或没有授权发布引用 |
| 409 | `resource_archived` | 重复归档或修改归档素材 |
| 409 | `asset_upload_expired` | 上传申请已过期 |
| 409 | `asset_object_missing` | 确认时对象存储对象不存在 |
| 409 | `asset_metadata_mismatch` | 确认时大小、MIME、SHA-256 或 ETag 不符合申请 |
| 409 | `asset_not_available` | 管理下载、引用或确认遇到不可用状态 |
| 409 | `asset_not_publishable` | 候选 Revision 含非 available 素材 |
| 409 | `idempotency_key_reused` | 同一幂等键用于不同规范请求 |
| 409 | `job_not_cancelable` | 任务状态不允许取消 |
| 409 | `job_not_retryable` | 任务状态或次数不允许重试 |
| 410 | `job_file_expired` | 错误报告或导出结果已过期 |
| 413 | `file_too_large` | 申请文件或实际对象超过配置上限 |
| 503 | `object_store_unavailable` | 对象存储临时不可用，可安全重试 |

- `object_store_unavailable` 可带 `Retry-After`，调用方可以在不更换幂等键的情况下重试原创建请求。
- CSV 行级错误使用报告和 `TransferError.code`，不把十万行详情放入顶层 `ErrorResponse.details`。
- `302` 的 `Location`、`304` 和成功 CSV 文件响应不是错误，不返回 `ErrorResponse`。

## 10. 迁移建议

阶段三迁移建议从 `000027` 起，由集成 Agent 最终分配。本文只给出建议，不创建迁移文件：

| 建议编号 | 目的 |
| --- | --- |
| `000027` | 建立素材元数据、状态和确认字段 |
| `000028` | 建立 Revision 级 `asset_references` |
| `000029` | 建立通用后台任务、幂等键、租约和取消字段 |
| `000030` | 建立任务文件元数据和过期索引 |
| `000031` | 建立 CSV 导入暂存行 |
| `000032` | 建立导入错误详情 |
| `000033` | 建立任务完成标记和清理索引 |

每个迁移仍须遵守 F0 的单语句、连续前缀和不可改写规则。现有 `000001` 至 `000026` 不修改；跨内容和素材模块的约束须由双方与集成 Agent 评审。

## 11. 实现与兼容门禁

- 验证对象 Key 不含用户文件名，上传签名绑定方法、MIME、`If-None-Match` 和 SHA-256 元数据，不绑定浏览器不可控的 `Content-Length`；确认逐项比较对象元数据并流式计算实际 SHA-256。
- 验证私有 Bucket 启动探测、签名过期、对象不存在和可重试故障映射，且凭证、Key 和签名 URL 不进入日志、审计或前端产物。
- 验证单媒体、多媒体、嵌套媒体、Revision 引用不可变性，以及 JSON、投影、关联、素材引用、指针和审计事务一致性。
- 验证候选 Revision 发布前重新校验素材；归档不改写历史和已发布内容，客户端只可下载授权发布 Revision 引用的素材。
- 验证两个 Worker 不重复领取、租约续期、租约丢失停止写入、崩溃回收、自动重试次数、取消安全点和幂等重放。
- 使用 100,000 行代表性 CSV 验证流式内存边界、暂存、全量校验、任一行失败正式内容零写入，以及成功时完整写入。
- 验证所有字段 CSV 编码、严格表头、UTF-8/BOM、RFC 4180 引号、对象与重复组 JSON、行级错误排序和截断。
- 验证导出筛选快照、稳定排序、UTF-8 BOM、对象存储流式写入、文件过期和下载时权限重检。
- F3 分片只有在实现、契约测试和集成 Agent 评审完成后，才可移除 `pending-implementation` 并聚合进根 OpenAPI。
- F3 完成后，本文路由、方法、DTO 必填字段、素材状态、媒体 JSON、CSV 编码、任务状态和稳定错误码只能做兼容扩展；破坏性修改必须形成新冻结决议。
